import { substances } from './substances';
import { saveEntry, setStreak, dateStr } from './storage';

export function initializeMockData() {
  // Mock data initialization disabled to ensure empty default state
}
